- 学习测p39 例16 
	- [[矩阵#^rank-1]]
	- [[矩阵#^elem-3]]
例16 已知矩阵 $\mathbf{A} = \begin{bmatrix} a & 1 & 1 \\ 1 & a & 1 \\ 1 & 1 & a \end{bmatrix}$ 和矩阵 $\mathbf{B} = \begin{bmatrix} 1 & 2 & -1 \\ 2 & 3 & 5 \\ 3 & 6 & -3 \end{bmatrix}$ 等价，则 $a = \_$ .
例16 【分析】 $A$ 和 $\pmb{B}$ 等价 $\Leftrightarrow A$ 经初等变换得到矩阵 $\pmb{B}$

  

$$

\Leftrightarrow \exists \text {可逆矩阵} P \text {和} Q \text {使} P A Q = B

$$

  

$$

\Leftrightarrow r (\mathbf {A}) = r (\mathbf {B}).

$$

  

明显 $r(\pmb {B}) = 2.$ 而

  

$$

| \textbf {A} | = \left| \begin{array}{c c c} a & 1 & 1 \\ 1 & a & 1 \\ 1 & 1 & a \end{array} \right| = (a + 2) (a - 1) ^ {2},

$$

  

当 $a = 1$ 时， $r(A) = 1$ .

  

当 $a = -2$ 时， $\mathbf{A}$ 中 $\left| \begin{array}{cc} -2 & 1 \\ 1 & -2 \end{array} \right| \neq 0$ ，而 $|\mathbf{A}| = 0$ ，从而 $r(\mathbf{A}) = 2$ .

  

∴A 和 B 等价 $\Leftrightarrow a = -2$ .

- 学习册p42 例19
例 19 已知 A, B 是 n 阶矩阵.

  

(1) $\begin{bmatrix} E & E \\ O & E \end{bmatrix} \begin{bmatrix} A & B \\ B & A \end{bmatrix} \begin{bmatrix} E & -E \\ O & E \end{bmatrix} = \underline{\hspace{2cm}}.$

  

(2) $\left|\begin{array}{ll}\mathbf{A}&\mathbf{B}\\ \mathbf{B}&\mathbf{A}\end{array}\right|= \_$ .
例 19 【分析】

  

(1) $\begin{bmatrix} E & E \\ O & E \end{bmatrix} \begin{bmatrix} A & B \\ B & A \end{bmatrix} \begin{bmatrix} E & -E \\ O & E \end{bmatrix} = \begin{bmatrix} A + B & A + B \\ B & A \end{bmatrix} \begin{bmatrix} E & -E \\ O & E \end{bmatrix}$

  

$$

= \left[ \begin{array}{c c} {\pmb {A} + \pmb {B}} & {\pmb {O}} \\ {\pmb {B}} & {\pmb {A} - \pmb {B}} \end{array} \right],

$$

  

(2) $\left|\begin{matrix}A&B\\B&A\end{matrix}\right|=\left|\begin{matrix}A+B&O\\B&A-B\end{matrix}\right|=|A+B|\cdot|A-B|.$
	- [[行列式#^det-6]]
- 求解变换的矩阵（练习册p19/14）
已知 $A = \begin{bmatrix} 1 & -1 & -1 & 3 \\ 2 & -1 & -3 & 1 \\ 3 & 2 & -5 & 2 \end{bmatrix}$ ，化其为行最简矩阵 F，并求一个可逆矩阵 P，使 PA = F.
【解】 对矩阵 $\mathbf{A}$ 作初等行变换

  

$$

\begin{array}{l} (\mathbf {A} \mid \mathbf {E}) = \left[ \begin{array}{c c c c c c c} 1 & - 1 & - 1 & 3 & 1 & 0 & 0 \\ 2 & - 1 & - 3 & 1 & 0 & 1 & 0 \\ 3 & 2 & - 5 & 2 & 0 & 0 & 1 \end{array} \right] \\ \rightarrow \left[\begin{array}{c c c c c c c}1&- 1&- 1&3&1&0&0\\0&1&- 1&- 5&- 2&1&0\\0&5&- 2&- 7&- 3&0&1\end{array}\right] \\ \rightarrow \left[\begin{array}{c c c c c c c}1&- 1&- 1&3&1&0&0\\0&1&- 1&- 5&- 2&1&0\\0&0&3&1 8&7&- 5&1\end{array}\right] \\ \rightarrow \left[\begin{array}{c c c c c c c}1&0&- 2&- 2&- 1&1&0\\0&1&- 1&- 5&- 2&1&0\\0&0&1&6&\frac {7}{3}&- \frac {5}{3}&\frac {1}{3}\end{array}\right] \\ \rightarrow \left[\begin{array}{c c c c c c c}1&0&0&1 0&\frac {1 1}{3}&- \frac {7}{3}&\frac {2}{3}\\0&1&0&1&\frac {1}{3}&- \frac {2}{3}&\frac {1}{3}\\0&0&1&6&\frac {7}{3}&- \frac {5}{3}&\frac {1}{3}\end{array}\right], \\ \end{array}

$$

  

矩阵 A 的行最简矩阵 $F = \begin{bmatrix} 1 & 0 & 0 & 10 \\ 0 & 1 & 0 & 1 \\ 0 & 0 & 1 & 6 \end{bmatrix}$ . $P = \frac{1}{3} \begin{bmatrix} 11 & -7 & 2 \\ 1 & -2 & 1 \\ 7 & -5 & 1 \end{bmatrix}$ 有 PA = F.
	- [[矩阵#^rlmat-3]]
- 练习册p31/14
已知 $\pmb{\alpha}_{1} = (1,4,0,2)^{\mathrm{T}}, \pmb{\alpha}_{2} = (2,7,1,3)^{\mathrm{T}}, \pmb{\alpha}_{3} = (0,1, - 1,a)^{\mathrm{T}}, \pmb{\beta} = (3,10,b,4)^{\mathrm{T}}$ ，问

  

(1)a,b 取何值时, $\beta$ 不能由 $\alpha_{1},\alpha_{2},\alpha_{3}$ 线性表出?

  

(2)a,b 取何值时, $\beta$ 可由 $\alpha_{1},\alpha_{2},\alpha_{3}$ 线性表出?并写出此表示式.
14.【解】设 $x_{1}\pmb{\alpha}_{1} + x_{2}\pmb{\alpha}_{2} + x_{3}\pmb{\alpha}_{3} = \pmb{\beta}$ . 因为对 $(\pmb{\alpha}_{1},\pmb{\alpha}_{2},\pmb{\alpha}_{3},\pmb{\beta})$ 作初等行变换有

  

$$

\left[\begin{array}{c c c c}1&2&0&3\\4&7&1&1 0\\0&1&- 1&b\\2&3&a&4\end{array}\right]\rightarrow \left[\begin{array}{c c c c}1&2&0&3\\0&- 1&1&- 2\\0&1&- 1&b\\0&- 1&a&- 2\end{array}\right]\rightarrow \left[\begin{array}{c c c c}1&0&2&- 1\\0&1&- 1&2\\0&0&a - 1&0\\0&0&0&b - 2\end{array}\right]

$$

  

所以

  

(1) 当 $b \neq 2$ 时, 线性方程组 $(\alpha_{1}, \alpha_{2}, \alpha_{3})x = \beta$ 无解, 此时 $\beta$ 不能由 $\alpha_{1}, \alpha_{2}, \alpha_{3}$ 线性表出.

  

(2) 当 $b = 2, a \neq 1$ 时，线性方程组 $(\alpha_{1}, \alpha_{2}, \alpha_{3})x = \beta$ 有唯一解，即

  

$$

\boldsymbol {x} = (x _ {1}, x _ {2}, x _ {3}) ^ {\mathrm{T}} = (- 1, 2, 0) ^ {\mathrm{T}}

$$

  

于是 $\beta$ 可唯一表示为 $\beta = -\alpha_{1} + 2\alpha_{2}$ .

  

当 $b = 2, a = 1$ 时，线性方程组 $(\alpha_{1}, \alpha_{2}, \alpha_{3})x = \beta$ 有无穷多个解，即

  

$$

\boldsymbol {x} = (x _ {1}, x _ {2}, x _ {3}) ^ {\mathrm{T}} = k (- 2, 1, 1) ^ {\mathrm{T}} + (- 1, 2, 0) ^ {\mathrm{T}}

$$

  

则 $\boldsymbol{\beta}=(-1-2k)\boldsymbol{\alpha}_{1}+(2+k)\boldsymbol{\alpha}_{2}+k\boldsymbol{\alpha}_{3},k$ 是任意实数.
	- 线性相关的向量组表示一个新向量不唯一，要解方程[[向量#^line-1]]
- 练习册p33/22
设 n 维列向量组 $\alpha_{1}, \cdots, \alpha_{m} (m < n)$ 线性无关，则 n 维列向量组 $\beta_{1}, \cdots, \beta_{m}$ 线性无关的充分必要条件为

  

(A) 向量组 $\alpha_{1},\cdots,\alpha_{m}$ 可由向量组 $\beta_{1},\cdots,\beta_{m}$ 线性表示.

  

(B) 向量组 $\beta_{1},\cdots,\beta_{m}$ 可由向量组 $\alpha_{1},\cdots,\alpha_{m}$ 线性表示.

  

(C) 向量组 $\alpha_{1},\cdots,\alpha_{m}$ 与向量组 $\beta_{1},\cdots,\beta_{m}$ 等价.

  

(D) 矩阵 $A = [\alpha_{1}, \cdots, \alpha_{m}]$ 与矩阵 $B = [\beta_{1}, \cdots, \beta_{m}]$ 等价.
答案：D
	- [[向量#线性组合、线性相关]]。线性无关的本质理解：线性无关就是向量组在空间中占用不同维度数的最大个数。
- 学习册p89/例6
例6 已知 $\mathbf{A}$ 是 $5\times 4$ 矩阵， $\pmb{\eta}_1,\pmb{\eta}_2$ 是 $Ax = 0$ 的基础解系，则 $A^{\mathrm{T}}y = 0$ 的基础解系

  

(A) 不存在.

  

(B) 有一个非零解.

  

(C) 有 2 个线性无关的解.

  

(D) 有 3 个线性无关的解.
答案：D
	- [[线性方程组#^qc-3]]
- 学习册p93/例13
例13 求和矩阵 $\mathbf{A} = \begin{bmatrix} 1 & 2 \\ -1 & 3 \end{bmatrix}$ 可交换的所有矩阵.
例13 【解】设 $X = \begin{bmatrix} x_1 & x_2 \\ x_3 & x_4 \end{bmatrix}$ 和矩阵 $\mathbf{A}$ 可交换，即 $AX = X\mathbf{A}$ ，

  

$$

\left[ \begin{array}{l l} 1 & 2 \\ - 1 & 3 \end{array} \right] \left[ \begin{array}{l l} x _ {1} & x _ {2} \\ x _ {3} & x _ {4} \end{array} \right] = \left[ \begin{array}{l l} x _ {1} & x _ {2} \\ x _ {3} & x _ {4} \end{array} \right] \left[ \begin{array}{l l} 1 & 2 \\ - 1 & 3 \end{array} \right],

$$

  

得 $\left\{ \begin{array}{cccc} & x_{2} + 2x_{3} & = 0,\\ 2x_{1} + 2x_{2} & -2x_{4} = 0,\\ -x_{1} & +2x_{3} + x_{4} = 0,\\ & x_{2} + 2x_{3} & = 0, \end{array} \right.$

  

$$

\pmb {B} = \left[\begin{array}{c c c c}{0}&{1}&{2}&{0}\\{2}&{2}&{0}&{- 2}\\{- 1}&{0}&{2}&{1}\\{0}&{1}&{2}&{0}\end{array}\right]\rightarrow \left[\begin{array}{c c c c}{1}&{0}&{- 2}&{- 1}\\{0}&{1}&{2}&{0}\\{0}&{0}&{0}&{0}\\{0}&{0}&{0}&{0}\end{array}\right],

$$

  

$4 - r(B) = 2$ , 取 $x_{3}, x_{4}$ 为自由变量.

  

令 $x_{3} = t, x_{4} = u$ ，得 $x_{1} = 2t + u, x_{2} = -2t$ ，所以 $\mathbf{X} = \begin{bmatrix} 2t + u & -2t \\ t & u \end{bmatrix}$ ，

  

$t, u$ 为任意常数.
	- 矩阵可交换[[矩阵的运算#^mul-2]]
- 学习册p95/例17
例 17 求一个齐次线性方程组,使它的基础解系为

  

$$

\boldsymbol {\eta} _ {1} = (4, 3, 1, 2) ^ {\mathrm{T}}, \boldsymbol {\eta} _ {2} = (0, 1, 3, - 2) ^ {\mathrm{T}}.

$$
例 17 【解】 设所求齐次方程组为 Ax = 0.

  

由 $n - r(A) = 2$ ，且 $n = 4$ ，知 $r(A) = 2$

  

又 $A(\pmb{\eta}_1, \pmb{\eta}_2) = \pmb{0}$ , 转置得

  

$$

\left[ \begin{array}{c} \boldsymbol {\eta} _ {1} ^ {\mathrm{T}} \\ \boldsymbol {\eta} _ {2} ^ {\mathrm{T}} \end{array} \right] \boldsymbol {A} ^ {\mathrm{T}} = \boldsymbol {O}.

$$

  

考查齐次方程组 $Bx = \begin{bmatrix} \eta_{1}^{T} \\ \eta_{2}^{T} \end{bmatrix} x = 0.$

  

由 $n-r(B)=4-2=2$ 可知, Bx=0 的基础解系是 $A^{T}$ 的列向量.

  

$$

\begin{array}{l} \boldsymbol {B} = \left[\begin{array}{l}\boldsymbol {\eta} _ {1} ^ {\mathrm{T}}\\\boldsymbol {\eta} _ {2} ^ {\mathrm{T}}\end{array}\right] = \left[\begin{array}{c c c c}4&3&1&2\\0&1&3&- 2\end{array}\right]\rightarrow \left[\begin{array}{c c c c}4&0&- 8&8\\0&1&3&- 2\end{array}\right] \\ \rightarrow \left[\begin{array}{c c c c}1&0&- 2&2\\0&1&3&- 2\end{array}\right]. \\ \end{array}

$$

  

令 $x_{3} = 1, x_{4} = 0$ ，解出 $x_{1} = 2, x_{2} = -3$ .

  

令 $x_{3} = 0, x_{4} = 1$ ，解出 $x_{1} = -2, x_{2} = 2$ .

  

于是 Bx = 0 的基础解系为

  

$$

(2, - 3, 1, 0) ^ {\mathrm{T}}, (- 2, 2, 0, 1) ^ {\mathrm{T}}.

$$

  

故 $\mathbf{A} = \begin{bmatrix} 2 & -3 & 1 & 0 \\ -2 & 2 & 0 & 1 \end{bmatrix}$ .

  

那么所求齐次方程组是

  

$$

\left\{ \begin{array}{l} 2 x _ {1} - 3 x _ {2} + x _ {3} = 0, \\ - 2 x _ {1} + 2 x _ {2} + x _ {4} = 0. \end{array} \right.

$$

  

【注】所求方程组是不唯一的.
	- 有解空间，反求齐次方程组
- 练习册p44/14
已知 $\alpha_{1}, \alpha_{2}, \alpha_{3}, \alpha_{4}$ 是线性方程组 $Ax = 0$ 的一个基础解系，若 $\beta_{1} = \alpha_{1} + t\alpha_{2}, \beta_{2} = \alpha_{2} + t\alpha_{3}, \beta_{3} = \alpha_{3} + t\alpha_{4}, \beta_{4} = \alpha_{4} + t\alpha_{1}$ ，讨论实数 $t$ 满足什么关系时， $\beta_{1}, \beta_{2}, \beta_{3}, \beta_{4}$ 也是 $Ax = 0$ 的一个基础解系。
【解】因 $\alpha_{1},\alpha_{2},\alpha_{3},\alpha_{4}$ 是 Ax=0 的解. 所以 $\beta_{1},\beta_{2},\beta_{3},\beta_{4}$ 也是 Ax=0 的解.

  

又 $(\pmb{\beta}_1, \pmb{\beta}_2, \pmb{\beta}_3, \pmb{\beta}_4) = (\pmb{\alpha}_1, \pmb{\alpha}_2, \pmb{\alpha}_3, \pmb{\alpha}_4)\left[ \begin{array}{cccc}1 & 0 & 0 & t\\ t & 1 & 0 & 0\\ 0 & t & 1 & 0\\ 0 & 0 & t & 1 \end{array} \right]$ ，而 $\left| \begin{array}{cccc}1 & 0 & 0 & t\\ t & 1 & 0 & 0\\ 0 & t & 1 & 0\\ 0 & 0 & t & 1 \end{array} \right| = 1 - t^4$

  

所以 $t \neq \pm 1$ 时， $\pmb{\beta}_{1}, \pmb{\beta}_{2}, \pmb{\beta}_{3}, \pmb{\beta}_{4}$ 线性无关。而 $Ax = 0$ 的基础解系由4个解向量构成。因此 $t \neq \pm 1$ 时 $\pmb{\beta}_{1}, \pmb{\beta}_{2}, \pmb{\beta}_{3}, \pmb{\beta}_{4}$ 也是 $Ax = 0$ 的一个基础解系。
	- 看似是向量组，其实应该看矩阵的变换。
- 练习册p47/22
已知非齐次线性方程组

  

$$

\left\{ \begin{array}{l} x _ {1} + x _ {2} + x _ {3} + x _ {4} = - 1, \\ 4 x _ {1} + 3 x _ {2} + 5 x _ {3} - x _ {4} = - 1, \\ a x _ {1} + x _ {2} + 3 x _ {3} + b x _ {4} = 1 \end{array} \right.

$$

  

有 3 个线性无关的解.

  

(1) 证明方程组系数矩阵 A 的秩 $r(A)=2$ ;

  

(2) 求 a, b 的值及方程组的通解.
【解】（1）设 $\alpha_{1},\alpha_{2},\alpha_{3}$ 是非齐次方程组的3个线性无关的解，那么 $\alpha_{1} - \alpha_{2},\alpha_{1} - \alpha_{3}$ 是 $Ax = 0$ 线性无关的解，所以 $n - r(A)\geqslant 2$ ，即 $r(A)\leqslant 2$

  

显然矩阵 $\mathbf{A}$ 中有二阶子式 $\left| \begin{array}{ll}1 & 1\\ 4 & 3 \end{array} \right|$ 不为0，又有 $r(\mathbf{A})\geqslant 2$ ，从而秩 $r(\mathbf{A}) = 2.$

  

(2) 对增广矩阵作初等行变换, 有

  

$$

\overline {{{\boldsymbol {A}}}} = \left[\begin{array}{c c c c c}1&1&1&1&- 1\\4&3&5&- 1&- 1\\a&1&3&b&1\end{array}\right]\rightarrow \left[\begin{array}{c c c c c}1&1&1&1&- 1\\0&- 1&1&- 5&3\\0&1 - a&3 - a&b - a&a + 1\end{array}\right]

$$

  

$$

\rightarrow \left[\begin{array}{c c c c c}1&1&1&1&- 1\\0&1&- 1&5&- 3\\0&0&4 - 2 a&b + 4 a - 5&4 - 2 a\end{array}\right]

$$

  

由题设和(Ⅰ)知, $r(\mathbf{A})=r(\overline{\mathbf{A}})=2$ ,故有

  

$$

4 - 2 a = 0, b + 4 a - 5 = 0

$$

  

解出 $a = 2, b = -3$ ，此时

  

$$

\overline {{{{\boldsymbol {A}}}}} \rightarrow \left[\begin{array}{c c c c c}1&0&2&- 4&2\\0&1&- 1&5&- 3\\0&0&0&0&0\end{array}\right]

$$

  

那么 $\pmb{\alpha} = (2, -3, 0, 0)^{\mathrm{T}}$ 是 $Ax = b$ 的解，且 $\pmb{\eta}_{1} = (-2, 1, 1, 0)^{\mathrm{T}}, \pmb{\eta}_{2} = (4, -5, 0, 1)$ 是 $Ax = 0$ 的基础解系，所以方程组的通解是 $\pmb{\alpha} + k_{1}\pmb{\eta}_{1} + k_{2}\pmb{\eta}_{2}$ （ $k_{1}, k_{2}$ 为任意常数）.
	- [[线性方程组#^not-qc-3]]，对于非齐次方程组秩和解空间维度的关系
- 学习册p119/例19
  

例 19 已知 A 是三阶实对称矩阵, 满足 $A^{2} = A$ , 若 $r(A - E) = 2$ , 则 A 的特征值为 ____.
例19 【分析】设 $A\alpha = \lambda \alpha$ ，则 $A^n\alpha = \lambda^n\alpha$ ，于是由 $A^2 = A$ 得 $A^2\alpha = A\alpha$ 即 $(\lambda^2 -\lambda)\alpha = 0.$

  

知矩阵 A 的特征值为 1 或 0.

  

因 A 是实对称矩阵, 知 $A \sim \Lambda$ , 那么 $A - E \sim \Lambda - E$ .

  

故 $r(\pmb{\Lambda} - \pmb{E}) = r(\pmb{A} - \pmb{E}) = 2$ ，所以 $\pmb{\Lambda} = \begin{bmatrix} 1 & & \\ & 0 & \\ & & 0 \end{bmatrix}$ ，即矩阵 $\pmb{A}$ 的特征值为： $1,0,0.$

  

【注】注意和例 8 的区别. 例 8 中不能求出 A 的具体特征值.
	- [[特征值和特征向量#实对称矩阵（ $A T=A$）]]，其中对于特征值的奇特解法
- 练习册p58/16/26/27/30
16.设矩阵 $A = \begin{bmatrix} a & -1 & c \\ 5 & b & 3 \\ 1 - c & 0 & -a \end{bmatrix}$ ，其行列式 $|A| = -1$ ，又 A 的伴随矩阵 $A^{*}$ 有一个特征值 $\lambda_{0}$ ，属于 $\lambda_{0}$ 的一个特征向量为 $\boldsymbol{\alpha} = (-1, -1, 1)^{\mathrm{T}}$ ，求 a, b, c 和 $\lambda_{0}$ 的值.
【解】因为 $\alpha$ 是 $A^{*}$ 属于特征值 $\lambda_{0}$ 的特征向量，即

  

$$

\mathbf {A} ^ {*} \boldsymbol {\alpha} = \lambda_ {0} \boldsymbol {\alpha} \tag {1}

$$

  

根据 $AA^{*}=|A|E$ 及已知条件 $|A|=-1$ ，用 A 左乘(1)式两端有

  

$$

- \alpha = \lambda_ {0} A \alpha

$$

  

即 $\lambda_0\left[ \begin{array}{ccc}a & -1 & c\\ 5 & b & 3\\ 1 - c & 0 & -a \end{array} \right]\left[ \begin{array}{c} - 1\\ -1\\ 1 \end{array} \right] = -\left[ \begin{array}{c} - 1\\ -1\\ 1 \end{array} \right]$

  

由此可得

  

$$

\left\{ \begin{array}{l} \lambda_ {0} (- a + 1 + c) = 1 \\ \lambda_ {0} (- 5 - b + 3) = 1 \\ \lambda_ {0} (- 1 + c - a) = - 1 \end{array} \right. \tag {2}

$$

  

(2)-(4) 得 $\lambda_0 = 1$ . 将 $\lambda_0 = 1$ 代入 (3) 得 $b = -3$ , 代入 (2) 得 $a = c$ .

  

由 $|\mathbf{A}| = -1$ 和 $a = c$ 有

  

$$

\left| \begin{array}{c c c} a & - 1 & a \\ 5 & - 3 & 3 \\ 1 - a & 0 & - a \end{array} \right| = a - 3 = - 1

$$

  

故 $a = c = 2$ 因此

  

$$

a = 2, b = - 3, c = 2, \lambda_ {0} = 1.

$$

26.已知三阶矩阵 A 与三维向量 x，使得向量组 x, Ax, $A^{2}x$ 线性无关，且满足 $A^{3}x = 3Ax - 2A^{2}x$ .

  

(1) 记 $P = [x, Ax, A^{2}x]$ ，求三阶矩阵 B，使 $A = PBP^{-1}$ ;

  

(2) 计算行列式 $\left| A + E \right|$ .
【解】（1）（方法一） 由于 $AP = PB$ ，即

  

$$

\mathbf {A} [ \mathbf {x}, \mathbf {A x}, \mathbf {A} ^ {2} \mathbf {x} ] = [ \mathbf {A x}, \mathbf {A} ^ {2} \mathbf {x}, \mathbf {A} ^ {3} \mathbf {x} ] = [ \mathbf {A x}, \mathbf {A} ^ {2} \mathbf {x}, 3 \mathbf {A x} - 2 \mathbf {A} ^ {2} \mathbf {x} ]

$$

  

$$

= [ \boldsymbol {x}, \boldsymbol {A} \boldsymbol {x}, \boldsymbol {A} ^ {2} \boldsymbol {x} ] \left[ \begin{array}{l l l} 0 & 0 & 0 \\ 1 & 0 & 3 \\ 0 & 1 & - 2 \end{array} \right]

$$

  

所以 $B = \begin{bmatrix} 0 & 0 & 0 \\ 1 & 0 & 3 \\ 0 & 1 & -2 \end{bmatrix}$ .

  

（方法二）由于 $P = [x, Ax, A^{2}x]$ 可逆，那么 $P^{-1}P = E$ ，即 $P^{-1}[x, Ax, A^{2}x] = E$ .

  

所以 $\pmb{P}^{-1}\pmb{x} = \begin{bmatrix} 1 \\ 0 \\ 0 \end{bmatrix}, \pmb{P}^{-1}\pmb{A}\pmb{x} = \begin{bmatrix} 0 \\ 1 \\ 0 \end{bmatrix}, \pmb{P}^{-1}\pmb{A}^2\pmb{x} = \begin{bmatrix} 0 \\ 0 \\ 1 \end{bmatrix}$

  

于是 $B = P^{-1}AP = P^{-1}[Ax,A^2x,A^3x]$

  

$$

\begin{array}{l} = \boldsymbol {P} ^ {- 1} \left[ \boldsymbol {A x}, \boldsymbol {A} ^ {2} \boldsymbol {x}, 3 \boldsymbol {A x} - 2 \boldsymbol {A} ^ {2} \boldsymbol {x} \right] \\ = \left[ P ^ {- 1} A x, P ^ {- 1} A ^ {2} x, P ^ {- 1} \left(3 A x - 2 A ^ {2} x\right) \right] \\ = \left[ \begin{array}{c c c} 0 & 0 & 0 \\ 1 & 0 & 3 \\ 0 & 1 & - 2 \end{array} \right]. \\ \end{array}

$$

  

(2) 由(1)知 $A \sim B$ ，那么 $A + E \sim B + E$ ，从而

  

$$

| \mathbf {A} + \mathbf {E} | = | \mathbf {B} + \mathbf {E} | = \left| \begin{array}{c c c} 1 & 0 & 0 \\ 1 & 1 & 3 \\ 0 & 1 & - 1 \end{array} \right| = - 4.

$$

27.设矩阵 $\mathbf{A} = \begin{bmatrix} 1 & 2 & -3 \\ -1 & 4 & -3 \\ 1 & a & 5 \end{bmatrix}$ 的特征方程有一个二重根，求 $a$ 的值，并讨论 $\mathbf{A}$ 是否可相似对角化.
【解】 $\mathbf{A}$ 的特征多项式为

  

$$

\begin{array}{l} | \lambda E - A | = \left| \begin{array}{c c c} \lambda - 1 & - 2 & 3 \\ 1 & \lambda - 4 & 3 \\ - 1 & - a & \lambda - 5 \end{array} \right| = \left| \begin{array}{c c c} \lambda - 2 & 2 - \lambda & 0 \\ 1 & \lambda - 4 & 3 \\ - 1 & - a & \lambda - 5 \end{array} \right| \\ = (\lambda - 2) \left(\lambda^ {2} - 8 \lambda + 1 8 + 3 a\right) \\ \end{array}

$$

  

若 $\lambda = 2$ 是特征方程的二重根，则有 $2^{2} - 16 + 18 + 3a = 0$ ，解得 $a = -2$

  

当 $a = -2$ 时， $\mathbf{A}$ 的特征值为 $2, 2, 6$ ，矩阵 $2\mathbf{E} - \mathbf{A} = \begin{bmatrix} 1 & -2 & 3 \\ 1 & -2 & 3 \\ -1 & 2 & -3 \end{bmatrix}$ 的秩为 $1$ ，故 $\lambda = 2$ 对应的线性无关的特征向量有两个，从而 $\mathbf{A}$ 可相似对角化.

  

若 $\lambda = 2$ 不是特征方程的二重根，则 $\lambda^2 - 8\lambda + 18 + 3a$ 为完全平方，从而 $18 + 3a = 16$ 解得 $a = -\frac{2}{3}$ .

  

当 $a = -\frac{2}{3}$ 时， $\mathbf{A}$ 的特征值为 2, 4, 4，矩阵 $4E - \mathbf{A} = \begin{bmatrix} 3 & -2 & 3 \\ 1 & 0 & 3 \\ -1 & \frac{2}{3} & -1 \end{bmatrix}$ 的秩为 2，故 $\lambda = 4$ 对应的线性无关的特征向量只有一个，从而 $\mathbf{A}$ 不可相似对角化.

30.设三阶实对称矩阵 $\mathbf{A}$ 的特征值 $\lambda_1 = 1, \lambda_2 = 2, \lambda_3 = -2$ ，且 $\alpha_1 = (1, -1, 1)^{\mathrm{T}}$ 是 $\mathbf{A}$ 的属于 $\lambda_1$ 的一个特征向量，记 $B = A^5 - 4A^3 + E$ ，其中 $\mathbf{E}$ 为三阶单位矩阵。

  

(1) 验证 $\alpha_{1}$ 是矩阵 B 的特征向量, 并求 B 的全部特征值与特征向量;

  

(2) 求矩阵 B.
【解】（1）由 $A\alpha = \lambda\alpha$ 知 $A^{n}\alpha = \lambda^{n}\alpha$ . 那么

  

$$

\boldsymbol {B} \boldsymbol {\alpha} _ {1} = (\boldsymbol {A} ^ {5} - 4 \boldsymbol {A} ^ {3} + \boldsymbol {E}) \boldsymbol {\alpha} _ {1} = \boldsymbol {A} ^ {5} \boldsymbol {\alpha} _ {1} - 4 \boldsymbol {A} ^ {3} \boldsymbol {\alpha} _ {1} + \boldsymbol {\alpha} _ {1} = (\lambda_ {1} ^ {5} - 4 \lambda_ {1} ^ {3} + 1) \boldsymbol {\alpha} _ {1} = - 2 \boldsymbol {\alpha} _ {1}

$$

  

所以 $\alpha_{1}$ 是矩阵 B 属于特征值 $\mu_{1} = -2$ 的特征向量.

  

类似地，若 $A\pmb{\alpha}_{2} = \lambda_{2}\pmb{\alpha}_{2}, A\pmb{\alpha}_{3} = \lambda_{3}\pmb{\alpha}_{3}$ ，有

  

$$

\boldsymbol {B} \boldsymbol {\alpha} _ {2} = (\lambda_ {2} ^ {5} - 4 \lambda_ {2} ^ {3} + 1) \boldsymbol {\alpha} _ {2} = \boldsymbol {\alpha} _ {2}, \boldsymbol {B} \boldsymbol {\alpha} _ {3} = (\lambda_ {3} ^ {5} - 4 \lambda_ {3} ^ {3} + 1) \boldsymbol {\alpha} _ {3} = \boldsymbol {\alpha} _ {3}

$$

  

因此，矩阵 $\pmb{B}$ 的特征值为 $\mu_{1} = -2, \mu_{2} = \mu_{3} = 1.$

  

由矩阵 $\mathbf{A}$ 是对称矩阵知矩阵 $\mathbf{B}$ 也是对称矩阵，设矩阵 $\mathbf{B}$ 属于特征值 $\mu = 1$ 的特征向量是 $\pmb{\beta} = (x_{1}, x_{2}, x_{3})^{\mathrm{T}}$ ，那么因为实对称矩阵特征值不同的特征向量相互正交，有

  

$$

\boldsymbol {\alpha} _ {1} ^ {\mathrm{T}} \boldsymbol {\beta} = x _ {1} - x _ {2} + x _ {3} = 0

$$

  

所以矩阵 B 属于特征值 $\mu = 1$ 的线性无关的特征向量是 $\boldsymbol{\beta}_{2} = (1, 1, 0)^{\mathrm{T}}, \boldsymbol{\beta}_{3} = (-1, 0, 1)^{\mathrm{T}}$ .

  

因而, 矩阵 B 属于特征值 $\mu_{1} = -2$ 的特征向量是 $k_{1}(1, -1, 1)^{\mathrm{T}}$ , 其中 $k_{1}$ 是不为 0 的任意常数.

  

矩阵 B 属于特征值 $\mu = 1$ 的特征向量是 $k_{2}(1,1,0)^{\mathrm{T}} + k_{3}(-1,0,1)^{\mathrm{T}}$ ，其中 $k_{2}, k_{3}$ 是不全为 0 的任意常数.

  

(2) 由 $B\alpha_{1} = -2\alpha_{1}, B\beta_{2} = \beta_{2}, B\beta_{3} = \beta_{3}$ 有 $B(\alpha_{1}, \beta_{2}, \beta_{3}) = (-2\alpha_{1}, \beta_{2}, \beta_{3})$ . 那么

  

$$

\begin{array}{l} \boldsymbol {B} = (- 2 \boldsymbol {\alpha} _ {1}, \boldsymbol {\beta} _ {2}, \boldsymbol {\beta} _ {3}) (\boldsymbol {\alpha} _ {1}, \boldsymbol {\beta} _ {2}, \boldsymbol {\beta} _ {3}) ^ {- 1} \\ = \left[ \begin{array}{c c c} - 2 & 1 & - 1 \\ 2 & 1 & 0 \\ - 2 & 0 & 1 \end{array} \right] \left[ \begin{array}{c c c} 1 & 1 & - 1 \\ - 1 & 1 & 0 \\ 1 & 0 & 1 \end{array} \right] ^ {- 1} = \left[ \begin{array}{c c c} - 2 & 1 & - 1 \\ 2 & 1 & 0 \\ - 2 & 0 & 1 \end{array} \right] \cdot \frac {1}{3} \left[ \begin{array}{c c c} 1 & - 1 & 1 \\ 1 & 2 & 1 \\ - 1 & 1 & 2 \end{array} \right] \\ = \left[ \begin{array}{c c c} 0 & 1 & - 1 \\ 1 & 0 & 1 \\ - 1 & 1 & 0 \end{array} \right]. \\ \end{array}

$$
	- 有难度的题目
- 练习册p59/21
设向量 $\boldsymbol{\alpha}=(a_{1},a_{2},\cdots,a_{n})^{\mathrm{T}},\boldsymbol{\beta}=(b_{1},b_{2},\cdots,b_{n})^{\mathrm{T}}$ 都是非零向量，且满足条件 $\alpha^{T}\beta=0$ ，记 n 阶矩阵 $A=\alpha\beta^{T}$ . 求：

  

(1) $A^{2}$ .

  

(2) 矩阵 A 的特征值和特征向量.
【解】（1）由 $A = \alpha\beta^{T}$ 和 $\alpha^{T}\beta = 0$ ，有

  

$$

\mathbf {A} ^ {2} = (\boldsymbol {\alpha} \boldsymbol {\beta} ^ {\mathrm{T}}) (\boldsymbol {\alpha} \boldsymbol {\beta} ^ {\mathrm{T}}) = \boldsymbol {\alpha} (\boldsymbol {\beta} ^ {\mathrm{T}} \boldsymbol {\alpha}) \boldsymbol {\beta} ^ {\mathrm{T}} = 0 \boldsymbol {\alpha} \boldsymbol {\beta} ^ {\mathrm{T}} = \boldsymbol {O}.

$$

  

(2) 设 $\lambda$ 是 A 的任一特征值, $\alpha$ 是 A 属于特征值 $\lambda$ 的特征向量, 即 $A\alpha = \lambda\alpha, \alpha \neq 0$ . 那么

  

$$

\mathbf {A} ^ {2} \boldsymbol {\alpha} = \lambda \mathbf {A} \boldsymbol {\alpha} = \lambda^ {2} \boldsymbol {\alpha}

$$

  

因为 $A^{2}=O$ ，故 $\lambda^{2}\alpha=0$ ，又因 $\alpha\neq0$ ，从而矩阵 A 的特征值是 $\lambda=0(n$ 重根 $)$ .

  

不妨设向量 $\alpha, \beta$ 的第1个分量 $a_1 \neq 0, b_1 \neq 0$ . 对齐次线性方程组 $(0E - A)x = 0$ 的系数矩阵作初等行变换，有

  

$$

- \mathbf {A} = \left[\begin{array}{c c c c}- a _ {1} b _ {1}&- a _ {1} b _ {2}&\dots&- a _ {1} b _ {n}\\- a _ {2} b _ {1}&- a _ {2} b _ {2}&\dots&- a _ {2} b _ {n}\\\vdots&\vdots&&\vdots\\- a _ {n} b _ {1}&- a _ {n} b _ {2}&\dots&- a _ {n} b _ {n}\end{array}\right]\rightarrow \left[\begin{array}{c c c c}b _ {1}&b _ {2}&\dots&b _ {n}\\0&0&\dots&0\\\vdots&\vdots&&\vdots\\0&0&\dots&0\end{array}\right]

$$

  

得到基础解系

  

$$

\boldsymbol {\alpha} _ {1} = (- b _ {2}, b _ {1}, 0, \dots , 0) ^ {\mathrm{T}}, \boldsymbol {\alpha} _ {2} = (- b _ {3}, 0, b _ {1}, \dots , 0) ^ {\mathrm{T}}, \dots , \boldsymbol {\alpha} _ {n - 1} = (- b _ {n}, 0, 0, \dots , b _ {1}) ^ {\mathrm{T}}

$$

  

于是矩阵 A 属于特征值 $\lambda = 0$ 的特征向量为 $k_{1}\alpha_{1} + k_{2}\alpha_{2} + \cdots + k_{n-1}\alpha_{n-1}$ ，其中 $k_{1}, k_{2}, \cdots, k_{n-1}$ 是不全为零的任意常数.
	- 如果 A是幂零矩阵即$A^k=0$，那么A所有特征值为0
	- 如果存在非零特征值 λ，那么 $A^kx=\lambda^kx$永不为0，因此和 $A^k=0$矛盾
- 练习册p69/6
用配方法化二次型

  

$$

f (x _ {1}, x _ {2}, x _ {3}) = x _ {1} x _ {2} - 2 x _ {1} x _ {3} + 4 x _ {2} x _ {3}

$$

  

为标准形,并写出所用可逆变换.
【解】令 $\left\{\begin{aligned}x_{1}&=y_{1}+y_{2},\\ x_{2}&=y_{1}-y_{2},\\ x_{3}&=\quad y_{3},\end{aligned}\right.$ 则

  

$$

f \left(x _ {1}, x _ {2}, x _ {3}\right) = \left(y _ {1} + y _ {2}\right) \left(y _ {1} - y _ {2}\right) - 2 \left(y _ {1} + y _ {2}\right) y _ {3}

$$

  

$$

+ 4 \left(y _ {1} - y _ {2}\right) y _ {3}

$$

  

$$

= y _ {1} ^ {2} - y _ {2} ^ {2} + 2 y _ {1} y _ {3} - 6 y _ {2} y _ {3}

$$

  

$$

= \left(y _ {1} ^ {2} + 2 y _ {1} y _ {3} + y _ {3} ^ {2}\right) - y _ {2} ^ {2} - 6 y _ {2} y _ {3} - y _ {3} ^ {2}

$$

  

$$

= \left(y _ {1} + y _ {3}\right) ^ {2} - \left(y _ {2} ^ {2} + 6 y _ {2} y _ {3} + 9 y _ {3} ^ {2}\right) - y _ {3} ^ {2} + 9 y _ {3} ^ {2}

$$

  

$$

= \left(y _ {1} + y _ {3}\right) ^ {2} - \left(y _ {2} + 3 y _ {3}\right) ^ {2} + 8 y _ {3} ^ {2}.

$$

  

令 $\left\{\begin{aligned}z_{1}&=y_{1}\quad+y_{3},\\ z_{2}&=y_{2}+3y_{3},\\ z_{3}&=y_{3},\end{aligned}\right.$ 即 $\left\{\begin{aligned}y_{1}&=z_{1}\quad-z_{3},\\ y_{2}&=z_{2}-3z_{3},\\ y_{3}&=z_{3},\end{aligned}\right.$ 得 $f(x_{1},x_{2},x_{3})=z_{1}^{2}-z_{2}^{2}+8z_{3}^{2}.$

  

所用坐标变换：

  

$$

\begin{array}{l} \left[ \begin{array}{l} x _ {1} \\ x _ {2} \\ x _ {3} \end{array} \right] = \left[ \begin{array}{c c c} 1 & 1 & 0 \\ 1 & - 1 & 0 \\ 0 & 0 & 1 \end{array} \right] \left[ \begin{array}{l} y _ {1} \\ y _ {2} \\ y _ {3} \end{array} \right] = \left[ \begin{array}{c c c} 1 & 1 & 0 \\ 1 & - 1 & 0 \\ 0 & 0 & 1 \end{array} \right] \left[ \begin{array}{c c c} 1 & 0 & - 1 \\ 0 & 1 & - 3 \\ 0 & 0 & 1 \end{array} \right] \left[ \begin{array}{l} z _ {1} \\ z _ {2} \\ z _ {3} \end{array} \right] \\ = \left[ \begin{array}{c c c} 1 & 1 & - 4 \\ 1 & - 1 & 2 \\ 0 & 0 & 1 \end{array} \right] \left[ \begin{array}{l} z _ {1} \\ z _ {2} \\ z _ {3} \end{array} \right]. \\ \end{array}

$$
	- 配方法化二次型[[二次型#^erci-1]]
- 练习册p70/15
设矩阵 $A = \begin{bmatrix} 1 & 0 & 1 \\ 0 & 2 & 0 \\ 1 & 0 & 1 \end{bmatrix}$ ，矩阵 $B = (kE + A)^{2}$ ，其中 k 为实数，E 为单位矩阵。求对角矩阵 $\Lambda$ ，使 B 与 $\Lambda$ 相似，并求 k 为何值时，B 为正定矩阵。
【解】由于 A 是实对称矩阵,有

  

$$

\pmb {B} ^ {\mathrm{T}} = \left[ (k \pmb {E} + \pmb {A}) ^ {2} \right] ^ {\mathrm{T}} = \left[ (k \pmb {E} + \pmb {A}) ^ {\mathrm{T}} \right] ^ {2} = (k \pmb {E} + \pmb {A}) ^ {2} = \pmb {B}

$$

  

即 B 是实对称矩阵, 故 B 必可相似对角化.

  

由

  

$$

| \lambda E - A | = \left| \begin{array}{c c c} \lambda - 1 & 0 & - 1 \\ 0 & \lambda - 2 & 0 \\ - 1 & 0 & \lambda - 1 \end{array} \right| = \lambda (\lambda - 2) ^ {2}

$$

  

可得到 A 的特征值是 $\lambda_{1} = \lambda_{2} = 2, \lambda_{3} = 0.$

  

那么 $kE + A$ 的特征值是 $k + 2, k + 2, k$ ，而 $(kE + A)^2$ 的特征值是 $(k + 2)^2, (k + 2)^2, k^2$ .

  

故 $\pmb{B}\sim \pmb{\Lambda} = \left[ \begin{array}{lll} (k + 2)^2 & & \\ & (k + 2)^2 & \\ & & k^2 \end{array} \right]$

  

因为矩阵 B 正定的充分必要条件是特征值全大于 0, 可见当 $k \neq -2$ 且 $k \neq 0$ 时, 矩阵 B 正定.
	- 利用定义求特征值
- 练习册p71/16
设矩阵 $\mathbf{A} = \begin{bmatrix} 2 & -1 & -1 \\ -1 & 2 & -1 \\ -1 & -1 & 2 \end{bmatrix}, \mathbf{B} = \begin{bmatrix} 1 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & 0 \end{bmatrix}$ , 则 $\mathbf{A}$ 与 $\mathbf{B}$
A) 合同且相似.

  

(B) 合同, 但不相似.

  

(C) 不合同,但相似.

  

(D) 既不合同,也不相似。
 B.

  

【解析】根据相似的必要条件: $\sum a_{\bar{n}} = \sum b_{\bar{n}}$ ，易见 $A$ 和 $B$ 肯定不相似，由此可排除(A)与(C).而合同的充分必要条件是有相同的正惯性指数、负惯性指数.为此可以用特征值加以判断.由

  

$$

| \lambda E - A | = \left| \begin{array}{c c c} \lambda - 2 & 1 & 1 \\ 1 & \lambda - 2 & 1 \\ 1 & 1 & \lambda - 2 \end{array} \right| = \left| \begin{array}{c c c} \lambda & \lambda & \lambda \\ 1 & \lambda - 2 & 1 \\ 1 & 1 & \lambda - 2 \end{array} \right| = \lambda (\lambda - 3) ^ {2}

$$

  

知矩阵 A 的特征值为 3, 3, 0. 故二次型 $x^{T}Ax$ 的正惯性指数 p = 2，负惯性指数 q = 0. 而二次型 $x^{T}Bx$ 的正惯性指数亦为 p = 2，负惯性指数 q = 0，所以 A 与 B 合同，故应选(B).
	- 合同的充要条件：[[二次型#^ht-1]]
- 399
已知 $A = \begin{bmatrix} 1 & -2 & 0 \\ 2 & 1 & 5 \\ 0 & 1 & 1 \end{bmatrix}$ ， $A^{*}$ 是 A 的伴随矩阵，则 $A^{*}x = 0$ 的通解是 ____.
答案$k_1(1,2,0)^T+k_2(-2,1,1)^T$
	- [[矩阵#伴随矩阵]]、[[线性方程组]]
- 400
设线性方程组 $A_{3 \times 3}x = b$ ，即

  

$$

\left\{ \begin{array}{l} a _ {1 1} x _ {1} + a _ {1 2} x _ {2} + a _ {1 3} x _ {3} = b _ {1} \\ a _ {2 1} x _ {1} + a _ {2 2} x _ {2} + a _ {2 3} x _ {3} = b _ {2} \\ a _ {3 1} x _ {1} + a _ {3 2} x _ {2} + a _ {3 3} x _ {3} = b _ {3} \end{array} \right. \tag {1}

$$

  

有唯一解 $\xi=(1,2,3)^{\mathrm{T}}$ .

  

方程组 $B_{3\times4}y=b$ ，即

  

$$

\left\{ \begin{array}{l} a _ {1 1} y _ {1} + a _ {1 2} y _ {2} + a _ {1 3} y _ {3} + a _ {1 4} y _ {4} = b _ {1} \\ a _ {2 1} y _ {1} + a _ {2 2} y _ {2} + a _ {2 3} y _ {3} + a _ {2 4} y _ {4} = b _ {2} \\ a _ {3 1} y _ {1} + a _ {3 2} y _ {2} + a _ {3 3} y _ {3} + a _ {3 4} y _ {4} = b _ {3} \end{array} \right. \tag {2}

$$

  

有特解 $\boldsymbol{\eta}=(-2,1,4,2)^{\mathrm{T}}$ ，则方程组(2)的通解是____。
答案$k_1(-3,-1,1,2)^T+k_2(-2,1,4,2)^T$
	- [[线性方程组]]
- 403
设 A 为三阶非零矩阵, $\alpha, \beta$ 为 3 维非零列向量, 满足 $r(A) = r(A^{2})$ , $A^{2}\alpha = 0$ , $\binom{A}{\alpha^{T}}\beta = 0$ , 则 $r(A) =$ ____.
答案：1
	- 线性方程组和解空间的深入理解
- 408
已知 $A \sim B$ ，其中 $B = \begin{bmatrix} 0 & 1 \\ 2 & 3 \end{bmatrix}$ ，则 $|A + 2E| =$ ____.
答案：8
	- 相似矩阵的结论
- 412、416
412 已知 A 是三阶实对称矩阵, 特征值是 1,3, -2, 其中 $\alpha_{1} = (1,2,-2)^{\mathrm{T}}, \alpha_{2} = (4,-1,a)^{\mathrm{T}}$ 分别是属于特征值 $\lambda = 1$ 与 $\lambda = 3$ 的特征向量, 那么矩阵 A 属于特征值 $\lambda = -2$ 的特征向量是 ____.
答案 $k_1(0,1,1)^T$
416

  

设 A 是三阶实对称矩阵, 存在正交阵 $Q = [\xi_{1}, \xi_{2}, \xi_{3}]$ , 使得 $Q^{-1}AQ = Q^{T}AQ =$

  

$$

\left[ \begin{array}{c c c} 1 & 0 & 0 \\ 0 & 2 & 0 \\ 0 & 0 & 3 \end{array} \right]

$$

  

,则矩阵 $\mathbf{B} = \mathbf{A} - \boldsymbol{\xi}_{1}\boldsymbol{\xi}_{1}^{\mathrm{T}}$ 的特征值是
答案：0，2，3
	- 对称矩阵不同特征值的特征向量正交
	- 正交矩阵中行向量为单位向量
- 413
已知 $P^{-1}AP = B$ ，其中 $B = \begin{bmatrix} 1 & -1 & 2 \\ 2 & -2 & 4 \\ 1 & -1 & 2 \end{bmatrix}$ ， $P = [\alpha_{1}, \alpha_{2}, \alpha_{3}]$ ，则矩阵 A 关于特征值 $\lambda = 0$ 的特征向量是 ____.

答案：$k_1(\alpha_1+\alpha_2)+k_2(-2\alpha_1+\alpha_3)$
	- 有点懵

- 422
已知多项式 $f(x)=\left|\begin{array}{cccc}x+1 & 1 & 1 & 2 \\ 1 & 2x+1 & 2x+1 & 4x+2 \\ 1 & x+1 & x+2 & 2x+4 \\ 2 & x+1 & 2x+2 & 3x+2\end{array}\right|$ ，则 $f(x)=0$ 根的个数为

(A)1. (B)2. (C)3. (D)4.

  

(A)1.

  

(B)2.

  

(C)3.

  

(D)4.
答案：C
	- 行列式按行按列展开，不能直接化简一行就得出结论，得化完。
- 434
下列命题中，

  

(1) 如果矩阵 AB = E，则 A 可逆且 $A^{-1} = B$ .

  

(2) 如果 $n$ 阶矩阵 $\mathbf{A}, \mathbf{B}$ 满足 $(\mathbf{AB})^2 = \mathbf{E}$ , 则 $(\mathbf{BA})^2 = \mathbf{E}$ .

  

(3) 如果矩阵 A, B 均 n 阶不可逆, 则 $A + B$ 必不可逆.

  

(4) 如果矩阵 A, B 均 n 阶不可逆, 则 AB 必不可逆.

  

正确的是

  

(A)(1)(2).

  

(B)(1)(4).

  

(C)(2)(3).

  

(D)(2)(4).
答案：D
	- 注意条件的题
- 447、448、450
447

  

已知 $A = \begin{bmatrix} 0 & 1 &  &  & a \\ 2 & 3 &  & a & 4 \\ 3 & 5 &  & 1 & 9 \end{bmatrix}$ ， $A^{\cdot}$ 是 A 的伴随矩阵，若 $r(A^{\cdot}) = 1$ ，则 a =

  

(A)3.

  

(B)2.

  

(C)1.

  

(D)1 或 3.
答案：D
448 设 $A, B$ 均 $n$ 阶矩阵, 且 $AB = A + B$ , 则

  

(1) 若 A 可逆, 则 B 可逆.

  

(2) 若 B 可逆, 则 $A + B$ 可逆.

  

(3) 若 $\pmb{B}$ 可逆, 则 $\pmb{A}$ 可逆.

  

(4) $A - E$ 恒可逆.

  

上述命题中,正确的命题共有

  

(A)1 个.

  

(B)2 个.

  

(C)3 个.

  

(D)4 个.
答案：D
450

  

已知 $A^{-1}=\begin{bmatrix}0&2&-1\\ 1&0&2\end{bmatrix}$ ，则 $|A|$ 的代数余子式 $A_{11}+A_{12}+A_{13}=$

  

(A) $\frac{1}{3}.$

  

(B) $\frac{2}{3}.$

  

(C)1.

  

(D)2.
答案：B
- 478
478

  

某五元齐次线性方程组经高斯消元,系数矩阵A化为 $\left[ \begin{array}{cccc}1 & -2 & 2 & 3 & -4\\ & & 1 & 5 & -2\\ & & & 2 & 0 \end{array} \right]$ ，选取自

  

由变量不能是

  

(A) $x_{2},x_{5}.$

  

(B) $x_{1}, x_{5}$ .

  

(C) $x_{3},x_{5}$ .

  

(D) $x_{2}, x_{3}$ .
答案：C
	- 自由变量
- 482
已知 $\alpha_{1}, \alpha_{2}, \alpha_{3}$ 是齐次方程组 Ax = 0 的基础解系，则 Ax = 0 的基础解系还可以是

  

(A) 与 $\alpha_{1}, \alpha_{2}, \alpha_{3}$ 等价的向量组.

  

(B) $\alpha_{1}-\alpha_{2},\alpha_{2}-\alpha_{3},\alpha_{3}-\alpha_{1}.$

  

(C) 与 $\alpha_{1}, \alpha_{2}, \alpha_{3}$ 等秩的向量组.

  

(D) $\alpha_{1},\alpha_{1} + \alpha_{2},\alpha_{1} + \alpha_{2} + \alpha_{3}$
答案：D
	- 基础解系和等价向量组
- 493、494、495
493 设 A 为三阶矩阵, 非零向量 $\alpha_{i}$ 是方程组 $Ax = (i - 1)\alpha_{i}$ 的解, 其中 i = 1, 2, 3, 则矩阵 $A^{2} + E$ 的迹为

  

(A)1.

  

(B)2.

  

(C)5.

  

(D)8.
答案：D
494 设 A 为三阶矩阵, 若 $r(E+A)=1, r(2E-A)=2$ , 则 $\mathrm{tr}(A^{2})$ 等于

  

(A)0.

  

(B)3.

  

(C)6.

  

(D)9.
答案：C
495 已知 A 是 n 阶可逆矩阵, 若 $A \sim B$ , 则下列命题中

  

① ${AB} \sim  {BA}$ .

  

② $A^{2}\sim B^{2}$ .

  

③ $A^{-1}\sim B^{-1}.$

  

④ $A^{T}\sim B^{T}.$

  

正确的命题共有

  

(A)4 个.

  

(B)3 个.

  

(C)2 个.

  

(D)1 个.
答案：A
- 496、497、499、500
496

  

设 A 是 n 阶矩阵, $A^{*}$ 是 A 的伴随矩阵, 已知 A 的每行元素之和为 k, $A^{*}$ 的每行元素 之和为 m, 则 $\left| A \right| =$

  

(A) ${km}$ .

  

(B) $(-1)^{n}km$ .

  

(C) $\frac{m}{k}$ .

  

(D) $(-1)^{n}\frac{k}{m}.$
答案：A
497 已知 A 是三阶矩阵, $r(A) = 1$ , 则 $\lambda = 0$

  

(A) 必是 A 的二重特征值.

  

(B) 至少是 A 的二重特征值.

  

(C) 至多是 A 的二重特征值.

  

(D) 一重、二重、三重特征值都有可能.
答案：B
499
设 $\boldsymbol{\alpha}=(1,-1,a)^{\mathrm{T}},\boldsymbol{\beta}=(1,a,2)^{\mathrm{T}},\boldsymbol{A}=\boldsymbol{E}+\boldsymbol{\alpha}\boldsymbol{\beta}^{\mathrm{T}}$ ，且 $\lambda=3$ 是矩阵 A 的特征值，其中 $k \neq 0$ , 则矩阵 A 属于特征值 $\lambda = 3$ 的特征向量是

  

(A) $k(1, - 1,1)^{\mathrm{T}}$ . (B) $k(1,1,2)^{\mathrm{T}}$

  

(C) $k(2,0,3)^{\mathrm{T}}$

  

(D) $k(0,2,1)^{\mathrm{T}}$
答案：A
500

  

设 A 是三阶矩阵, 其特征值是 1,3, -2, 相应的特征向量依次为 $\alpha_{1}, \alpha_{2}, \alpha_{3}$ , 若 $P = [\alpha_{1}, 2\alpha_{3}, -\alpha_{2}]$ ，则 $P^{-1}AP =$

  

(A) $\left[ \begin{array}{ccc}1 &  &  \\  & -2 &  \\  &  & 3 \end{array} \right].$

  

(B) $\left[ \begin{array}{ccc}1 &  &  \\  & -4 &  \\  &  & -3 \end{array} \right].$

  

(C) $\left[ \begin{array}{ccc}1 &  &  \\  & -2 &  \\  &  & -3 \end{array} \right].$

  

(D) $\left[ \begin{array}{ccc}1 &  &  \\  & 3 &  \\  &  & -2 \end{array} \right].$
答案：A
# 行列式
## 范德蒙行列式
361
(1) $\left| \begin{array}{cccc}0 & 2 & 2 & 2\\ 2 & 0 & 2 & 2\\ 2 & 2 & 0 & 2\\ 2 & 2 & 2 & 0 \end{array} \right| = \_$ .
答案：-48
  

(2) $\left| \begin{array}{cccc}1 & 2 & 3 & 4\\ 1 & 2^2 & 3^2 & 4^2\\ 1 & 2^3 & 3^3 & 4^3\\ 9 & 8 & 7 & 6 \end{array} \right| = \_$ .
答案：-120
## 爪形行列式
364
$n$ 阶行列式 $D =$

  

$$

\left| \begin{array}{c c c c c c c} n & n - 1 & n - 2 & \dots & 3 & 2 & 1 \\ 0 & 0 & 0 & \dots & 0 & 2 & 2 \\ 0 & 0 & 0 & \dots & 3 & 0 & 3 \\ \vdots & \vdots & \vdots & & \vdots & \vdots & \vdots \\ 0 & 0 & n - 2 & \dots & 0 & 0 & n - 2 \\ 0 & n - 1 & 0 & \dots & 0 & 0 & n - 1 \\ n & 0 & 0 & \dots & 0 & 0 & n \end{array} \right|

$$

  

= ____.
答案：$(-1)^{\frac{n(n-1)}{2}}(1-\sum_{k=2}^nk)n!$
## 利用行变换矩阵求行列式
365、366
365 设 $A$ 为三阶矩阵,互换矩阵 $A$ 的 1,2 行,再将第 1 行的 2 倍加到第 2 行得矩阵 $B$ ,若 $|A|=3$ ,则 $|A+B|=$ ____.
答案：12
366 设 n 阶矩阵 $A = [\alpha_{1}, \alpha_{2}, \cdots, \alpha_{n}]$ , $B = [\alpha_{n}, \alpha_{1}, \cdots, \alpha_{n-1}]$ ，若行列式 $|A| = 1$ ，则 $|A - B| =$ ____.
答案：0
## 等价矩阵-等价向量组
[[向量#等价矩阵和等价向量组]]
391
391 已知向量组

  

(I) $\pmb{\alpha}_{1} = (1,2,1)^{\mathrm{T}},\pmb{\alpha}_{2} = (2,3,a)^{\mathrm{T}},\pmb{\alpha}_{3} = (1,a + 2, - 2)^{\mathrm{T}},$

  

(Ⅱ) $\pmb{\beta}_{1} = (1, - 1,1)^{\mathrm{T}},\pmb{\beta}_{2} = (1,a, - 1)^{\mathrm{T}},\pmb{\beta}_{3} = (a,1,2)^{\mathrm{T}}$

  

不等价,则 a = ____.
答案：3;4;-1
## 过渡矩阵
[[向量#过渡矩阵]]
395
已知 $\alpha_{1}, \alpha_{2}, \alpha_{3}$ 是三维向量空间的一个基底, 若

  

$$

\boldsymbol {\beta} _ {1} = \alpha_ {1} + \alpha_ {2} + \alpha_ {3}, \boldsymbol {\beta} _ {2} = 3 \alpha_ {2} + \alpha_ {3}, \boldsymbol {\beta} _ {3} = \alpha_ {1} - \alpha_ {2},

$$

  

则由基 $\alpha_{1}, \alpha_{2}, \alpha_{3}$ 到基 $\beta_{1}, \beta_{2}, \beta_{3}$ 的过渡矩阵是 ____.
答案：$\left[ \begin{array}{ccc}1 &  0&  1\\  1& 3 & -1 \\ 1 & 1 & 0 \end{array} \right].$

