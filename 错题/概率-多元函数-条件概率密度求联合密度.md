---
type: 错题
domain: 概率论
topic: 多元函数概率的求法
source: 学习册p47/例5
wrong_count: 1
difficulty:
last_review:
mastery: 未掌握
knowledge: [条件概率密度, 边缘密度, 联合密度]
tags: [多元函数, 条件密度, 边缘密度]
---

# 条件概率密度求联合密度与边缘密度

## 题目
> 设 $(X,Y)$ 是二维随机变量，$X$ 的边缘概率密度函数为
> $$f_X(x) = \begin{cases} 3x^2, & 0 < x < 1, \\ 0, & \text{其他}. \end{cases}$$
> 在给定 $X = x(0 < x < 1)$ 的条件下，$Y$ 的条件概率密度函数为
> $$f_{Y|X}(y \mid x) = \begin{cases} \frac{3y^2}{x^3}, & 0 < y < x, \\ 0, & \text{其他}. \end{cases}$$
>
> (1) 求 $(X,Y)$ 的概率密度函数 $f(x,y)$ .
> (2) 求 Y 的边缘概率密度函数 $f_Y(y)$ .
> (3) 求 $P\{X > 2Y\}$ .

## 正确答案
> **(1)** $f_{Y|X}(y \mid x) = \frac{f(x,y)}{f_X(x)}$，其中 $f_X(x) > 0$（即 $0 < x < 1$）。
>
> $$f(x, y) = f_X(x) f_{Y|X}(y \mid x) = \begin{cases} \frac{9y^2}{x}, & 0 < y < x, \\ 0, & \text{其他}, \end{cases} \quad 0 < x < 1.$$
>
> 验证 $\int_0^1\int_0^x \frac{9y^2}{x} \mathrm{d}y\mathrm{d}x = 1$，故延拓为全平面：
>
> $$f(x, y) = \begin{cases} \frac{9y^2}{x}, & 0 < y < x < 1, \\ 0, & \text{其他}. \end{cases}$$
>
> **【评注】** $f(x,y)=f_X(x)f_{Y|X}(y|x)$ 只在 $f_X(x)>0$ 时成立，不能直接写成全平面形式。需先用条件密度的定义域推导，再通过积分为1延拓。
>
> **(2)** 
> $$f_Y(y) = \int_{-\infty}^{+\infty} f(x,y) \mathrm{d}x = \begin{cases} \int_y^1 \frac{9y^2}{x} \mathrm{d}x, & 0<y<1, \\ 0, & \text{其他} \end{cases}$$
> $$= \begin{cases} -9y^2 \ln y, & 0 < y < 1, \\ 0, & \text{其他}. \end{cases}$$
>
> **(3)**
> $$P\{X > 2Y\} = \iint_{x > 2y} f(x,y) \mathrm{d}x\mathrm{d}y = \int_0^1 \mathrm{d}x \int_0^{\frac{x}{2}} \frac{9y^2}{x} \mathrm{d}y$$
> $$= \int_0^1 \frac{3x^2}{8} \mathrm{d}x = \frac{1}{8}.$$

## 错因
-

## 我的理解
> 本题解题过程中用了4个"其他"，每个含意不同，需区分。关键是 $f(x,y)=f_X(x)f_{Y|X}(y|x)$ 仅在有定义的范围成立，最终要延拓成全平面并验证积分为1。

## 关联错题
-
